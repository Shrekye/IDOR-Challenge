<!DOCTYPE html>
<html>
<head>
  <title>IDOR Challenge</title>
</head>
<body>
  <h1>Bienvenue <%= user ? user.username : 'Invité' %></h1>
  <% if (user) { %>
    <a href="/logout">Logout</a>
    <a href="/users/<%= user._id %>">Mon profil</a>
    <% if (user.role === 'admin') { %>
      <p>Tu es admin !</p>
    <% } %>
  <% } else { %>
    <a href="/login">Login</a>
    <a href="/register">Register</a>
  <% } %>
</body>
</html>
